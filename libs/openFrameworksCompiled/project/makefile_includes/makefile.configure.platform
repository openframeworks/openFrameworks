ifndef PLATFORM_OS 
    # determine from the uname if not defined manually
    PLATFORM_OS=$(shell uname -s)
endif

ifdef MAKEFILE_DEBUG
    $(info =============================config.platform==================================)
    $(info PLATFORM_OS=$(PLATFORM_OS))
endif
