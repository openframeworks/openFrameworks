# change this path, if you are in a non standard directory
MAKEFILE_INCLUDES_PATH=../makefile_includes

# call the core.user file
include config.user.raspberrypi

# call the platform specific makefile
include config.platform.raspberrypi

# call the platform utility to determine the current platform specific variables
include $(MAKEFILE_INCLUDES_PATH)/Makefile.platform.utility

# call the shared core makefile
include $(MAKEFILE_INCLUDES_PATH)/Makefile.core
