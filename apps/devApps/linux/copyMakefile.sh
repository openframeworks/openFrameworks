for f in $(ls -d ../../examples/*); do cp makefile $f; cp config.make $f; done
for f in $(ls -d ../../addonsExamples/*); do cp makefile $f; cp config.make $f; done
for f in $(ls -d ../../androidExamples/*); do cp makefile $f; cp config.make $f; done
